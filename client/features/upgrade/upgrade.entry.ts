import "@tabler/icons-webfont/dist/tabler-icons.min.css";
import "./foundr-upgrade";

/**
 * Entry point for the plan and upgrade page at /upgrade.
 * Auth-guarded inside the component, like every other signed-in page.
 */
