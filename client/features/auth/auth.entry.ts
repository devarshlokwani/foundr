import "@tabler/icons-webfont/dist/tabler-icons.min.css";
import "./foundr-auth";

/**
 * Entry point for the sign-in and sign-up pages.
 * The <foundr-auth> component's mode attribute (set in each HTML file)
 * decides which view shows.
 */